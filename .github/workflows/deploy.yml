name: Deployment requires an authorized operational pass
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  contract:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'No automatic deployment. Follow docs/deployment.md with exact SHA/artifact.'
          echo 'Verified target, encrypted backup, ingress hooks and PRE certification required.'
